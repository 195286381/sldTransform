/**
 * @author zte10191772
 * @description define config's parameters validate middleware functions
 *
 * midllreware function as follow example:
 * function middlewareExample(params, addErrorInfo)
 * params is a object params which is object 
 * addErrorInof is a params which is function
 */
const middlewares = {
  checkFileName: function() {

  }
}

// mocha 是 javascript 的运行框架.

module.exports = middlewares